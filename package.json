{
  "name": "editrion",
  "version": "0.0.2-1",
  "description": "A modern code editor built with Tauri",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "ensure:icons": "node scripts/ensure-icons.cjs",
    "icons:tauri": "tauri icon src-tauri/icons/icon.png",
    "preview": "vite preview",
    "tauri": "tauri",
    "tauri:build:ci": "CI=1 tauri build",
    "dist:mac": "bash scripts/sign-notarize-mac.sh",
    "dist:mac:fast": "SKIP_NOTARIZE=1 bash scripts/sign-notarize-mac.sh",
    "notarize:submit": "bash scripts/notary-submit.sh",
    "notarize:finalize": "bash scripts/notary-finalize.sh",
    "tauri:build:mac": "CI=true tauri build",
    "tauri:build:mac:app": "CI=true tauri build --bundles app",
    "tauri:build:mac:arm64": "tauri build --target aarch64-apple-darwin",
    "tauri:build:mac:x64": "tauri build --target x86_64-apple-darwin",
    "tauri:build:mac:both": "npm run tauri:build:mac:arm64 && npm run tauri:build:mac:x64"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.4.0",
    "@tauri-apps/plugin-dialog": "^2.3.3",
    "monaco-editor": "^0.45.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.4.0",
    "typescript": "^5.0.0",
    "vite": "^5.0.0"
  }
}
